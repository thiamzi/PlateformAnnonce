{% extends "layout/layout.html.twig" %}

{% block titre %}
  Lecture d'une annonce - {{ parent() }}
{% endblock %}

{% block corps_plat %}
{% for message in app.flashes('info') %}
    <div style="text-align : center; margin-bottom  : 10px;font-size : 20px;
     background : green ; color : white  ; border-radius : 7px 7px 7px 7px ; " class="flash-success">
       {{ message }}
    </div>
{% endfor %}
  <h2></h2>
  <div class="list-group-item-heading">
    <div style="display: inline-block;margin-left: 5%;">
      <h3>{% trans %}Nom{% endtrans %} : {{app.user.nom }}
      <h3>{% trans %}Adresse Email{% endtrans %} : {{app.user.email}}</h3>
      </h3>
    </div>

  <p>
    <a href="{{ path('accueil' , {'page' : 1}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      {% trans %}Retour à la liste{% endtrans %}
    </a>
    <a href="{{ path('modifier_profile')}}" class="btn btn-default">
      <i class="glyphicon glyphicon-edit"></i>
      {% trans %}Modifier profile{% endtrans %}
    </a>
  </p>

{% endblock %}