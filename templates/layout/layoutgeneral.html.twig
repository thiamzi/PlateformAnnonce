<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <title>{% block title %}plateforme d'annonces{% endblock %}</title>

  {% block stylesheets %}
    {# On charge le CSS de bootstrap depuis le site directement #}
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
  {% endblock %}
</head>

<body>
  <div class="container">
    <div id="header" class="jumbotron">
      <h1>{% trans %}Ma plateforme d'annonces{% endtrans %}</h1>
      <p>
        {% trans %}Ce projet est propulsé par Symfony,
        et construit grâce au MOOC OpenClassrooms et SensioLabs.{% endtrans %}
      </p>
      <p>
        <a class="btn btn-primary btn-lg" href="https://openclassrooms.com/courses/developpez-votre-site-web-avec-le-framework-symfony2">
          {% trans %}Participer au MOOC »{% endtrans %}
        </a>
      </p>
      {% if app.user %}
        <h2>
          {% trans %}Bienvenue{% endtrans %} {{app.user.nom}}
        </h2>
      {% endif %}
    </div>

    <div class="row">
      <div id="menu" class="col-md-3">
        <h3>Menu</h3>
        <ul class="nav nav-pills nav-stacked">
          <li><a href="{{ path('accueil' , {'page' : 1}) }}">{% trans %}Accueil{% endtrans %}</a></li>
          {% if not is_granted('ROLE_AUTEUR') and not is_granted('ROLE_ADMIN')%}
            <li><a href="{{ path('loginn') }}">{% trans %}Se connecter{% endtrans %}</a></li>
          {% endif %}
          {% if is_granted('ROLE_AUTEUR') %}
            <li><a href="{{ path('mon_profile') }}">{% trans %}Mon profile{% endtrans %}</a></li>
            <li><a href="{{ path('anonce_mesAnonces' , {'page' : 1}) }}">{% trans %}Mes annonces{% endtrans %}</a></li>
            <li><a href="{{ path('anonce_ajout') }}">{% trans %}Ajouter une annonce{% endtrans %}</a></li>
            <li><a href="{{ path('logout') }}">{% trans %}Se deconnecter{% endtrans %}</a></li>
          {% endif %}
        </ul>

        <h4>{% trans %}Dernières annonces{% endtrans %}</h4>
        {{ render(controller("App\\Controller\\LayoutController::DernierAnonces", {'limit': 5})) }}
      </div>
      <div id="content" class="col-md-9">
        {% block body %}
        {% endblock %}
      </div>
    </div>

    <hr>

    <footer>
      <div class="pull-right">
      <p>The sky's the limit © {{ 'now'|date('Y') }} and beyond.</p>
      </div>
    </footer>
  </div>
  {% block javascripts %}
    {# Ajoutez ces lignes JavaScript si vous comptez vous servir des fonctionnalités du bootstrap Twitter #}
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script src="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
  {% endblock %}

</body>
</html>